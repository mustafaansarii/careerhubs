spring:
  application:
    name: USER-SERVICE
  mail:
    host: smtp.gmail.com
    port: 587
    username: mmduecell@gmail.com
    password: grui jbsq bxel qwyi
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  datasource:
    url: jdbc:mysql://localhost:3306/Task_auth_service?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: Mustafa@_1992
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true

server:
  port: 5001

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8070/eureka/

email:
  host-user: mmduecell@gmail.com
  host-password: grui jbsq bxel qwyi

jwt:
  secret: your-256-bit-secret-key-must-be-at-least-32-characters-long
  expiration: 3600000 # 1 hour
  refreshExpiration: 86400000 # 24 hours

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

verification-url: http://localhost:5000/user-service/api/auth/verify?token=