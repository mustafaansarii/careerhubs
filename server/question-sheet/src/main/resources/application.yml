spring:
  application:
    name: question-sheet
  mail:
    host: smtp.gmail.com
    port: 587
    username: mmduecell@gmail.com
    password: grui jbsq bxel qwyi
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  datasource:
    url: jdbc:mysql://localhost:3306/question_sheet?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: Mustafa@_1992
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true

server:
  port: 5002

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8070/eureka/




jwt:
  secret: your-256-bit-secret-key-must-be-at-least-32-characters-long
  expiration: 3600000 # 1 hour
  refreshExpiration: 86400000 # 24 hours

